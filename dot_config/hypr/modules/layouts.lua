----------------
---- LAYOUTS ---
----------------

-- Dwindle layout
hl.config({
    dwindle = {
        preserve_split = true,
    },
})

-- Master layout
hl.config({
    master = {
        new_status = "master",
    },
})

-- Scrolling layout
hl.config({
    scrolling = {
        fullscreen_on_one_column = true,
    },
})
