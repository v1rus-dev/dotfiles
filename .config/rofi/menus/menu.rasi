/* Compact variant of the main theme for the dmenu action menus. */
@import "~/.config/rofi/theme.rasi"

window {
    width: 440px;
    padding: 18px;
}

listview {
    lines: 6;
    scrollbar: false;
}

element {
    padding: 9px 12px;
}
